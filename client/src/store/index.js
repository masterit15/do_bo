import { createStore } from 'vuex'
import files from './modules/files.modules.js'
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    files
  }
})
